{
  "name": "dropi-global-deals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Ireland-first landed-cost comparison engine for cross-border shopping.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check public/app.js && node --check lib/landed-cost.mjs"
  }
}
